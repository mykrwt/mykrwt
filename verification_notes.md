# Field Notes Portal Verification

The root route presents the hand-drawn Field Notes aperture with visible circular routes, annotations, directional marks, and the `come in` entry control. The live interaction was opened once in-browser and completed its aperture transition to the `myk.rwt / Mayank Rawat` image-free profile state. The revealed profile retains the same drawn route motif and provides active social destinations, copy-link feedback, and the compact sound control.

## Sigil Gate Verification

The revised loading route now presents the supplied black ink sigil alone against a clean white field, with one small `click` control. Activation begins from a center seam and initiates the two-door gate motion; the completed reveal is checked separately after its scheduled gate duration.

## Original Shape Minimal Reset Verification

The supplied reference image has been removed from the live interface. The root route now shows a small original code-drawn line mark on the continuous deep-paper background, followed by one lowercase `click` prompt. The mark is visible in the live route before interaction and its loading stroke sequence remains present for ordinary visitors.

Activating the mark completed the short split reveal and landed on a page containing only the Mayank Rawat label, `myk.rwt` name, one bio paragraph, and four link destinations. No uploaded artwork, music control, photo, dashboard panel, or other extraneous content remains in the live profile.

## Expanded Pattern Verification

The enhanced loading route now presents a large original orbital crest with layered curved wings, contour loops, cross-routes, orbit rings, and a central core. The live loader remains centered, visually substantial, and retains the single `click` cue; the final responsive profile content has not grown beyond the requested name, bio, and links.

## Medieval Ink Sigil Verification

The revised live loader begins with only the arch, confirming there is no completed crest behind the animation. It then writes the wayfinder seal through its symbolic stages: the central spine and eye, the seal rings, four direction points, the bottom star, and final endpoints. The lowercase `click` control appears only after the final ink marks have settled.

## Stillroom Verification

The fresh loader is a standalone Stillroom calibration chamber rather than a sigil. At rest it presents the large suspended optical sphere, three offset wireframe planes, orbit markers, a scan beam, coordinate labels, and an `enter` cue. The live browser confirms that these elements are visible together on the dark deep-paper ground before interaction.
